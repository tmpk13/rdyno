{
  "name": "rdyno",
  "version": "0.0.1",
  "main": "./dist/extension.js",
  "devDependencies": {
    "@types/iarna__toml": "^2.0.5",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.110.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "esbuild": "^0.27.3",
    "eslint": "^9.39.3",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.1"
  },
  "activationEvents": [],
  "categories": [
    "Other"
  ],
  "contributes": {
    "commands": [
      {
        "command": "embeddedRust.selectBoard",
        "title": "Embedded Rust: Select Board"
      },
      {
        "command": "embeddedRust.build",
        "title": "Embedded Rust: Build"
      },
      {
        "command": "embeddedRust.flash",
        "title": "Embedded Rust: Flash"
      },
      {
        "command": "embeddedRust.rtt",
        "title": "Embedded Rust: RTT Monitor"
      },
      {
        "command": "embeddedRust.newProject",
        "title": "Embedded Rust: New Project"
      }
    ],
    "configuration": {
      "title": "Embedded Rust Runner",
      "properties": {
        "embeddedRust.boardConfigDir": {
          "type": "string",
          "default": ".rdyno",
          "description": "Directory containing board TOML configs"
        },
        "embeddedRust.probersPath": {
          "type": "string",
          "default": "probe-rs",
          "description": "Path to probe-rs binary"
        },
        "embeddedRust.pickerGlobs": {
          "type": "array",
          "items": { "type": "string" },
          "default": ["src/**/*.rs"],
          "description": "Glob patterns (relative to workspace root) for the file picker"
        },
        "embeddedRust.boardLibraryRepo": {
          "type": "string",
          "default": "tmpk13/rdyno",
          "description": "GitHub repo (owner/repo) to use as the board library, e.g. \"my-org/rdyno-boards\""
        }
      }
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "embeddedRust",
          "title": "Embedded Rust",
          "icon": "./imgs/dyno.svg"
        }
      ]
    },
    "views": {
      "embeddedRust": [
        {
          "type": "webview",
          "id": "embeddedRust.panel",
          "name": "Board Controls"
        },
        {
          "type": "webview",
          "id": "embeddedRust.newProject",
          "name": "New Project"
        },
        {
          "type": "webview",
          "id": "embeddedRust.boardLibrary",
          "name": "Board Library"
        }
      ]
    }
  },
  "description": "Rust embedded runner",
  "displayName": "rdyno",
  "engines": {
    "vscode": "^1.110.0"
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "dependencies": {
    "@iarna/toml": "^3.0.0"
  }
}